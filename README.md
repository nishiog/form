# 書類作成フォームビルダー

assetsフォルダ内のJSONデータからHTMLフォームを自動生成するツールです。

## 👥 対象ユーザー

- **Windows利用者（Go環境なし）**: [Windows利用者向けマニュアル](docs/windows-manual.md) を参照
- **開発者（Go環境あり）**: このREADMEを参照

## 📚 ドキュメント

### 利用者向け
- [Windows利用者向けマニュアル](docs/windows-manual.md) - Windows環境での使い方
- [Power Automate連携ガイド](docs/power-automate-setup.md) - 🔥 推奨: Microsoft Power Automateとの連携
- [クイックスタートガイド](docs/quick-start.md) - 3ステップで始める
- [配布パッケージ説明書](docs/README-DISTRIBUTION.md) - パッケージの内容

### 開発者向け
- [リリースプロセス](docs/release-process.md) - リリース手順
- [ドキュメント一覧](docs/README.md) - すべてのドキュメント

## ファイル構成

```
form/
├── assets/               # JSONデータファイル
│   ├── case.json        # 案件データ
│   ├── documents.json   # 書類データ
│   ├── fields.json      # 入力フィールドデータ（required、sample含む）
│   ├── case_documents.json    # 案件と書類の紐づけ
│   ├── document_fields.json   # 書類とフィールドの紐づけ
│   ├── config.json      # 設定ファイル（API URL等）※gitignore対象
│   └── config.json.example    # 設定ファイルのテンプレート
├── template.html        # HTMLテンプレート
├── build.go            # ビルドプログラム
├── go.mod              # Go モジュール定義
├── .gitignore          # Git除外設定
└── output/             # 生成されたHTMLの出力先 ※gitignore対象
    └── index.html      # 生成されたフォーム
```

## 使い方

### 0. 初期設定（初回のみ）

設定ファイルをコピーして作成：
```bash
cd /Users/7milch/Work/git/nishiog/form
cp assets/config.json.example assets/config.json
# config.jsonを編集してAPI URLとシークレットキーを設定
vim assets/config.json
```

### 1. ビルドの実行

```bash
cd /Users/7milch/Work/git/nishiog/form
go run build.go
```

### 2. 生成されたHTMLを確認

```bash
open output/index.html
```

## データ構造

### case.json（案件データ）
```json
[
  {
    "id": 1,
    "category": "定時株主総会",
    "path": ["定時株主総会", "取締役会設置会社", "会計監査"],
    "name": "定時株主総会 > 取締役会設置会社 > 会計監査"
  }
]
```

### documents.json（書類データ）
```json
[
  {
    "id": "minutes_regular_meeting",
    "name_ja": "定時株主総会議事録",
    "name_en": "Minutes of Regular Shareholders' Meeting"
  }
]
```

### fields.json（フィールドデータ）
```json
[
  {
    "id": "company_name",
    "label_ja": "会社名",
    "label_en": "Company Name",
    "type": "text",
    "required": false,
    "sample": "株式会社サンプル",
    "priority": 100
  }
]
```

### config.json（設定ファイル）
```json
{
  "api": {
    "post_url": "https://api.example.com/documents/submit",
    "method": "POST",
    "headers": {
      "Content-Type": "application/json"
    },
    "secret_key": "your-secret-key-here"
  },
  "app": {
    "title": "書類作成フォーム",
    "autosave_interval_ms": 500,
    "enable_console_log": true
  }
}
```

### case_documents.json（案件と書類の紐づけ）
```json
[
  {
    "case_id": 1,
    "document_ids": [
      "minutes_regular_meeting",
      "financial_statements"
    ]
  }
]
```

### document_fields.json（書類とフィールドの紐づけ）
```json
[
  {
    "document_id": "minutes_regular_meeting",
    "field_ids": [
      "company_name",
      "preparation_date",
      "meeting_date"
    ]
  }
]
```

## 機能

### 案件選択モード
- プルダウンから案件を選択
- 必要な書類が自動で選択される
- 重複フィールドは自動マージされる

### 手動選択モード
- 書類を個別に選択可能
- 同じフィールドは1回だけ入力

### 入力フォーム
- フィールドがマージされて重複入力を回避
- 各フィールドの下に使用される書類名をタグ表示
- **必須/任意の設定**（fields.jsonのrequiredで制御）
- **入力サンプル表示**（placeholderとして表示）
- バリデーション機能（必須チェック、型チェック）
- LocalStorageで自動保存

### データ送信
- **設定可能なAPI URL**（config.jsonで設定）
- JSON形式でPOST送信またはコンソール出力
- 書類ごとに必要なフィールドを含む
- デバッグ用のコンソールログのON/OFF可能

#### 送信されるJSONの例

```json
{
  "secret": "your-secret-key-here",
  "case_id": 1,
  "mode": "case",
  "documents": [
    {
      "document_id": "minutes_regular_meeting",
      "document_name": "定時株主総会議事録",
      "fields": {
        "company_name": "株式会社サンプル",
        "preparation_date": "2024-12-14",
        "head_office_address": "東京都千代田区丸の内一丁目1番1号",
        "representative_director_name": "山田太郎",
        "director_name": "佐藤次郎",
        "statutory_auditor_name": "鈴木三郎",
        "fiscal_year_start": "2024-04-01",
        "fiscal_year_end": "2025-03-31",
        "meeting_date": "2024-06-28",
        "meeting_time": "10:00",
        "meeting_place": "東京都千代田区丸の内一丁目1番1号 本社会議室",
        "attending_shareholders": "5",
        "attending_shares": "10000",
        "agenda_items": "第1号議案 事業報告承認の件\n第2号議案 取締役選任の件",
        "resolution_details": "第1号議案は全会一致で承認された。\n第2号議案は賛成多数により可決された。"
      }
    },
    {
      "document_id": "financial_statements",
      "document_name": "財務諸表",
      "fields": {
        "company_name": "株式会社サンプル",
        "preparation_date": "2024-12-14",
        "fiscal_year_start": "2024-04-01",
        "fiscal_year_end": "2025-03-31",
        "capital_amount": "10000000"
      }
    },
    {
      "document_id": "business_report",
      "document_name": "事業報告",
      "fields": {
        "company_name": "株式会社サンプル",
        "preparation_date": "2024-12-14",
        "representative_director_name": "山田太郎",
        "fiscal_year_start": "2024-04-01",
        "fiscal_year_end": "2025-03-31",
        "purpose_of_business": "1. ソフトウェアの開発及び販売\n2. 情報処理サービス業\n3. 経営コンサルティング業"
      }
    }
  ]
}
```

**フィールド説明：**
- `secret`: API認証用のシークレットキー（`config.json`の`api.secret_key`から取得）
- `case_id`: 選択した案件のID（案件選択モードの場合）
- `mode`: `"case"`（案件選択モード）または `"manual"`（手動選択モード）
- `documents`: 選択された書類の配列
  - `document_id`: 書類の識別子（英語）
  - `document_name`: 書類名（日本語）
  - `fields`: 入力されたフィールドのオブジェクト
    - **キー**: フィールドID（英語、例: `company_name`, `meeting_date`）
    - **値**: 入力された値（文字列）

**フィールドID一覧（主なもの）：**
- `company_name`: 会社名
- `preparation_date`: 作成日
- `head_office_address`: 本店所在地
- `representative_director_name`: 代表取締役氏名
- `director_name`: 取締役氏名
- `meeting_date`: 総会開催日
- `fiscal_year_start`: 事業年度開始日
- `fiscal_year_end`: 事業年度終了日
- `capital_amount`: 資本金の額
- `agenda_items`: 議題
- `resolution_details`: 決議事項

※ すべてのフィールドIDは `assets/fields.json` で確認できます。

**注意事項：**
- 同じフィールド（例：`company_name`）が複数の書類で使われる場合、入力は1回だけで済みますが、送信データには各書類ごとに同じ値が含まれます
- 空の入力フィールドは空文字列 `""` として送信されます
- 日付は `YYYY-MM-DD` 形式、数値は文字列として送信されます

## カスタマイズ

### 新しい案件を追加
`assets/case.json`に案件を追加し、`assets/case_documents.json`で必要な書類を紐づけます。

### 新しい書類を追加
1. `assets/documents.json`に書類を追加
2. `assets/document_fields.json`で必要なフィールドを紐づけ

### 新しいフィールドを追加
1. `assets/fields.json`にフィールドを追加
2. `assets/document_fields.json`で書類に紐づけ

### テンプレートの修正
`template.html`を編集して、デザインや機能を変更できます。

### API設定の変更
`assets/config.json`でAPI URLや送信設定をカスタマイズ：
- `api.post_url`: 送信先API URL
- `api.method`: HTTPメソッド（デフォルト: POST）
- `api.headers`: リクエストヘッダー
- `api.secret_key`: API認証用のシークレットキー（送信JSONに含まれる）
- `app.enable_console_log`: コンソールログの有効化（true/false）
- `app.autosave_interval_ms`: 自動保存の間隔（ミリ秒）

### フィールドの設定
`assets/fields.json`で各フィールドの設定を変更：
- `required`: true で必須、false で任意（デフォルトはすべてfalse）
- `sample`: 入力例として表示される文字列（placeholderとして表示）
- `priority`: 表示順序の優先度（数値が大きいほど上に表示、0-100推奨）
  - 100: 最優先（会社名など）
  - 90-95: 重要項目（本店所在地、作成日など）
  - 50-80: 通常項目
  - 10: デフォルト優先度

## ビルド後の配布

生成された`output/index.html`は単一ファイルで完結しているため、そのまま配布・使用できます。

## 開発環境

- Go 1.21以降
- 標準ライブラリのみ使用（外部依存なし）

## 配布用パッケージのビルド

### Windows利用者向けパッケージ

このプロジェクトは、Go環境を持たないWindows利用者向けに、ビルド済み実行ファイルを配布します。

### GitHub Actionsでの自動ビルド

リリースタグをプッシュすると、自動的にWindows用実行ファイルがビルドされます：

```bash
# バージョンタグを作成
git tag v1.0.0
git push origin v1.0.0
```

GitHub Actionsが以下を実行します：
1. Windows用の `form-builder.exe` をビルド
2. 必要なファイルをパッケージング
3. `form-builder-windows.zip` を作成
4. Releaseページに自動アップロード

### 手動ビルド（オプション）

ローカルでWindows用実行ファイルをビルドする場合：

```bash
# Windows用にビルド
GOOS=windows GOARCH=amd64 go build -o form-builder.exe build.go

# パッケージを作成
mkdir -p package/assets package/output
cp form-builder.exe template.html package/
cp assets/*.json package/assets/
cp assets/config.json.example package/assets/
rm -f package/assets/config.json
cp -r docs package/

# ZIPファイルを作成
cd package && zip -r ../form-builder-windows.zip . && cd ..
```

### Windows利用者向けマニュアル

配布パッケージには以下のマニュアルが含まれています：
- `docs/windows-manual.md` - 詳細な利用マニュアル
- `docs/quick-start.md` - クイックスタートガイド
- `docs/README-DISTRIBUTION.md` - パッケージ説明書

## セキュリティに関する注意事項

### 設定ファイルの管理

1. **config.jsonは.gitignoreに含まれています**
   - `assets/config.json`は機密情報（`secret_key`）を含むため、Gitにコミットされません
   - 代わりに`assets/config.json.example`をテンプレートとして使用してください

2. **初回セットアップ**
   ```bash
   cp assets/config.json.example assets/config.json
   # 本番環境用のシークレットキーを設定
   vim assets/config.json
   ```

3. **生成されたHTMLについて**
   - `output/index.html`には`config.json`の内容が埋め込まれます
   - `secret_key`も平文で含まれるため、適切なアクセス制御が必要です
   - 本番環境では、HTMLファイルの配布先を制限してください

4. **推奨事項**
   - 環境ごとに異なる`secret_key`を使用してください
   - 定期的に`secret_key`をローテーションしてください
   - `output/`ディレクトリもGitに含まれません（生成物のため）

