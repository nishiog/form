# リリースプロセス

## 📦 Windows利用者向けパッケージのリリース手順

### 前提条件

- GitHubリポジトリへのプッシュ権限
- リリースを作成する権限

### リリースの流れ

#### 1. コードの準備

変更をコミットしてプッシュ：

```bash
git add .
git commit -m "Release v1.0.0の準備"
git push origin main
```

#### 2. バージョンタグの作成

セマンティックバージョニング（v[メジャー].[マイナー].[パッチ]）を使用：

```bash
# 例: v1.0.0
git tag v1.0.0

# タグをプッシュ
git push origin v1.0.0
```

#### 3. GitHub Actionsの自動実行

タグをプッシュすると、GitHub Actionsが自動的に：

1. **ビルド環境のセットアップ**
   - Ubuntu最新版
   - Go 1.21

2. **Windows用実行ファイルのビルド**
   - `GOOS=windows GOARCH=amd64`
   - 出力: `form-builder.exe`

3. **パッケージの作成**
   - 実行ファイル
   - テンプレートファイル
   - assetsフォルダ（config.json除く）
   - ドキュメント
   - README.txt

4. **ZIPアーカイブの作成**
   - `form-builder-windows.zip`

5. **GitHubリリースの作成**
   - タグに対応するリリースページを自動作成
   - ZIPファイルを添付
   - リリースノートを自動生成

#### 4. リリースの確認

1. GitHubの「Releases」ページにアクセス
2. 新しいリリースが作成されていることを確認
3. `form-builder-windows.zip` がダウンロード可能か確認

#### 5. リリースノートの編集（オプション）

自動生成されたリリースノートを編集：

1. Releasesページで該当リリースを開く
2. 「Edit release」をクリック
3. リリースノートを編集：
   - 新機能
   - バグ修正
   - 破壊的変更
   - 既知の問題

例：
```markdown
## v1.0.0 - 2024-12-14

### 🎉 新機能
- 案件選択モードの追加
- 入力フィールドの自動マージ機能
- priority による表示順序制御

### 🐛 バグ修正
- 日付フィールドのバリデーション修正

### 📝 ドキュメント
- Windows利用者向けマニュアルを追加
- クイックスタートガイドを追加

### ⬆️ アップグレード方法
1. 新しいZIPファイルをダウンロード
2. 既存の `assets/config.json` を新しいフォルダにコピー
3. `form-builder.exe` を実行
```

### バージョン番号の決め方

セマンティックバージョニング: `v[メジャー].[マイナー].[パッチ]`

- **メジャー**: 破壊的変更（v1.0.0 → v2.0.0）
  - API形式の大幅変更
  - 設定ファイル形式の変更
  
- **マイナー**: 新機能追加（v1.0.0 → v1.1.0）
  - 新しい案件タイプの追加
  - 新機能の追加
  
- **パッチ**: バグ修正（v1.0.0 → v1.0.1）
  - バグ修正
  - ドキュメントの修正
  - 小さな改善

### リリース前のチェックリスト

- [ ] すべてのテストが通る
- [ ] READMEが最新の状態
- [ ] config.json.exampleが正しい
- [ ] ドキュメントが更新されている
- [ ] CHANGELOG.mdを更新（ある場合）
- [ ] バージョン番号が適切

### トラブルシューティング

#### GitHub Actionsが失敗する

**原因**: ビルドエラー、設定ミス

**確認事項**:
1. Actionsタブでログを確認
2. Go 1.21でローカルビルドが成功するか確認
3. `.github/workflows/release.yml` の設定を確認

#### リリースが作成されない

**原因**: タグの形式が正しくない

**解決方法**:
- タグ名が `v` で始まっているか確認（例: `v1.0.0`）
- タグをリモートにプッシュしたか確認

```bash
git push origin v1.0.0
```

#### ZIPファイルが不完全

**原因**: パッケージング時のパス指定ミス

**解決方法**:
1. ローカルで手動ビルドしてテスト
2. GitHub Actionsのログを確認
3. `.github/workflows/release.yml` のパスを確認

### 手動リリース（緊急時）

GitHub Actionsが使えない場合の手動リリース手順：

```bash
# 1. Windows用にビルド
GOOS=windows GOARCH=amd64 go build -o form-builder.exe build.go

# 2. パッケージディレクトリを作成
mkdir -p package/assets package/output package/docs

# 3. ファイルをコピー
cp form-builder.exe template.html package/
cp assets/*.json package/assets/
cp assets/config.json.example package/assets/
rm -f package/assets/config.json
cp -r docs/* package/docs/

# 4. README.txtを作成
cat > package/README.txt << 'EOF'
書類作成フォームビルダー - Windows版
使い方: docs/quick-start.md を参照
EOF

# 5. ZIPファイルを作成
cd package && zip -r ../form-builder-windows.zip . && cd ..

# 6. GitHubのReleasesページから手動でアップロード
```

### リリース後のフォロー

1. **ユーザーへの通知**
   - 新バージョンをアナウンス
   - 主要な変更点を説明

2. **フィードバックの収集**
   - Issuesをモニタリング
   - ユーザーからの質問に回答

3. **次のリリースの計画**
   - フィードバックに基づく改善
   - 新機能のロードマップ

## 📝 リリース履歴の管理

### CHANGELOG.mdの作成（推奨）

プロジェクトルートに `CHANGELOG.md` を作成：

```markdown
# Changelog

## [1.0.0] - 2024-12-14
### Added
- 初回リリース
- 案件選択モード
- 手動選択モード
- Windows利用者向けパッケージ

## [Unreleased]
### Added
- 予定している機能
```


